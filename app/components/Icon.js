import React from 'react';
import { View } from 'react-native';

function Icon({
    name,
    size,
    backgroundColor
}) {
    return (
        <View style={}>

        </View>
    );
}

export default Icon;